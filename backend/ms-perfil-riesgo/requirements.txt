# Extras de MS PerfilRiesgo sobre la imagen base (solventa/flask-base).
# Caché de datos externos (ASR2).
redis==5.2.1
# Cliente HTTP hacia las fuentes externas (Open Data / Open Finance).
requests==2.32.3
